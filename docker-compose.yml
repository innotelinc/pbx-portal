services:
  pbx:
    build: .
    container_name: pbx-portal
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - pbx-data:/app/data
    env_file:
      - .env
    environment:
      - NODE_ENV=production

volumes:
  pbx-data:
